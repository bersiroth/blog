{% extends ":Admin:layout.html.twig" %}

{% block body %}
    <a href="{{ path('admin_bersi_blog_add_category') }}" class="btn btn-primary pull-right" role="button">Ajouter</a>
    <br>
    <br>
    <br>
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            {% for category in liste_categories %}
            {% if loop.first %}
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
            </thead>
            <tbody>
            {% endif %}
            <tr>
                <td>{{ category.id }}</td>
                <td><a href="{{ path('admin_bersi_blog_edit_category', { 'id': category.id}) }}">{{ category.name }}</a></td>
            </tr>
            {% else %}
                <tr>
                    <td>Pas d'article trouvé.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
