{% extends ":Admin:layout.html.twig" %}

{% block body %}
    <a href="{{ path('admin_bersi_blog_add_tag') }}" class="btn btn-primary pull-right" role="button">Ajouter</a>
    <br>
    <br>
    <br>
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            {% for tag in liste_categories %}
            {% if loop.first %}
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
            </thead>
            <tbody>
            {% endif %}
            <tr>
                <td>{{ tag.id }}</td>
                <td><a href="{{ path('admin_bersi_blog_edit_tag', { 'id': tag.id}) }}">{{ tag.name }}</a></td>
            </tr>
            {% else %}
                <tr>
                    <td>Pas d'article trouvé.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
